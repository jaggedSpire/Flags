<Window x:Class="Flags.Poland"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Flags"
        mc:Ignorable="d"
        Title="Poland" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="Crimson" TargetType="{x:Type Canvas}">
            <Setter Property="Background" Value="#FFD1243D"/>
        </Style>
        <Style x:Key="White" TargetType="{x:Type Canvas}">
            <Setter Property="Background" Value="#E9E8E7"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Canvas Grid.Column="0"
                Grid.Row="0"
                Style="{DynamicResource White}"/>
        <Canvas Grid.Column="0"
                Grid.Row="1"
                Style="{DynamicResource Crimson}"/>
    </Grid>
</Window>
